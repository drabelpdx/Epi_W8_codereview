
{{#link-to 'new-answer' this.id class="btn btn-success"}}Provide an answer{{/link-to}}
<button class="btn btn-primary" {{action 'edit'}}>Edit this question</button>
<button class="btn btn-danger" {{action 'delete'}}>Delete this question</button>

<br>
<br>

{{#if isEditing}}
  <form>
    <div class="form-group">
      <label for="asked">Ask your question: </label>
      {{textarea id="asked" class="form-control" rows="2" value=asked}}
    </div>

    <div class="form-group">
      <label for="description">Provide more details: </label>
      {{textarea id="description" class="form-control" rows="5" value=description}}
    </div>

    <div class="form-group">
      <label for="author">Name: </label>
      {{input id="author" class="form-control" value=author}}
    </div>

    <button class="btn btn-primary" {{action 'save'}}>Save</button>
  </form>

{{else}}

    <div class="col-md-8">
      <h3>{{model.asked}}</h3><br>
      <h4>Details: {{model.description}}</h4><br>
      <h5>Author:  {{model.author}}</h5>

    </div>


{{/if}}

<div class="row">
<div class="col-md-4">
</div>
<div class="col-md-8">


<br><br>
<h3>Answers</h3>

  {{#each answers as |answer|}}
    <div class="row">
      <div class="col-md-2">
        {{#link-to 'answer' answer.id}}
          <button bind-attr class=":btn btn-sm btn-danger toDelete" {{action 'deleteAnswer'}}>Delete</button>
        {{/link-to}}
      </div>
      <div class="col-md-6">
        <h4>{{answer.text}}<br><small class='muted'>{{answer.name}}</small></h4><br>
      </div>
      <br>
    </div>
    <br>
  {{/each}}

  {{outlet}}
</div>

</div>
